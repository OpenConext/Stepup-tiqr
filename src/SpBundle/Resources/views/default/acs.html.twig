{% extends 'base.html.twig' %}

{% block title %}
    {{ 'page.acs.title'|trans }}
{% endblock %}

{% block page_heading %}
    {{ 'page.acs.sub_title'|trans }}
{% endblock %}

{% block body %}
    <div class="content-container">
        <div class="alert">
            <a href="{{ path('sp_demo') }}">Terug naar demo sp</a>
        </div>
        <table>
            <tbody>
            <tr>
                <td>{{ 'saml.request_id'|trans }}</td>
                <td>{{ requestId }}</td>
            </tr>
            <tr>
                <td>{{ 'saml.subject_name_id'|trans }}</td>
                <td>{% include '@Sp/default/table.html.twig' with {values: nameId} %}</td>
            </tr>
            <tr>
                <td>{{ 'saml.issuer'|trans }}</td>
                <td>{{ issuer }}</td>
            </tr>
            <tr>
                <td>{{ 'saml.authenticating_authority'|trans }}</td>
                <td>{% include '@Sp/default/table.html.twig' with {values: authenticatingAuthority} %}</td>
            </tr>
            <tr>
                <td>{{ 'saml.relay_state'|trans }}</td>
                <td>
                    {% if relayState is empty %}
                        <i class="fa fa-ban" aria-hidden="true"></i>
                    {% else %}
                        {{ relayState }}
                    {% endif %}
                </td>
            </tr>
            </tbody>
        </table>

        <h2>SAMLResponse</h2>
        <div class='alert'>
            <pre style="overflow-x: scroll;">{{ xml }}</pre>
        </div>
    </div>
{% endblock %}
